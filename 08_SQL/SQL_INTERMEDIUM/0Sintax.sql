/* 

    -----Datatypes-----

        NOT NULL        (Asegura que una columna no pueda tener un valor null).

        UNIQUE          (Asegura que todos los valores de una columna sean diferentes).

        PRIMARY KEY     (Permite identificar una clave de forma unica).

        FOREIGN KEY     (Sirve para crear una relacion con otra tabla).

        CHECK           (Se usa para asegurar que los valroes de una columna cumplan una condicion especifica).

        DEFAULT         (Asigna un valor por defecto a una columna).

        CONSTRAINT      (Permite definir una restriccion en una columna o grupo de columnas).

        CREATE INDEX    (Se usa para crear un indice en una o mas columnas de una tabla).

        AUTO_INCREMENT  (Permite aumentar el valor automaticamente cada vez que inserte una nueva fila).

        CREATE INDEX    (Se usa para crear un indice en una o mas columnas de una tabla).



    -----DATA TYPES-----

        DATE        (format YYYY-MM-DD).

        DATETIME    (format YYYY-MM-DD HH:MM:SS).

        TIMESTAMP   (format YYYY-MM-DD HH:MM:SS).

        TEAR        (format YYYY or YY).

*/