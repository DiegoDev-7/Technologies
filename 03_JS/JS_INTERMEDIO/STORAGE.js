/* 

    localStorage                    (Persiste incluso cuando se cierra el navegador).

    sessionsStorage                 (Se borra cuando se cierra la pestaña o el navegador).

    .setItem(key, value)            (Guarda un valor en el LocalStorage).

    .getItem(key)                   (Obtiene un valor del LocalStorage).

    .removeItem(key)                (Elimina un valor del LocalStorage).

    .clear()                        (Elimina todos los valores del LocalStorage).

    .key()                          (Obtiene la clave de un valor del LocalStorage).
    
*/